using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CylinderItem : Item
{
    // Start is called before the first frame update
    void Start()
    {
        itemName = "cylinder";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
