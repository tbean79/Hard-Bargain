using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UnicornItem : Item
{
    // Start is called before the first frame update
    void Start()
    {
        itemName = "ceramic unicorn";        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
