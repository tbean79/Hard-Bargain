using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SupremeTeeItem : Item
{
    // Start is called before the first frame update
    void Start()
    {
        itemName = "supreme tee";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
