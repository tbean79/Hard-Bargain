using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlanketItem : Item
{
    // Start is called before the first frame update
    void Start()
    {
        itemName = "coutour blanket";   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
